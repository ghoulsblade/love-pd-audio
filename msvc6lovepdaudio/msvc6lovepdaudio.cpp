// msvc6lovepdaudio.cpp : Definiert den Einsprungpunkt für die DLL-Anwendung.
//

#include "stdafx.h"
#include "msvc6lovepdaudio.h"

/*
BOOL APIENTRY DllMain( HANDLE hModule, 
                       DWORD  ul_reason_for_call, 
                       LPVOID lpReserved
					 )
{
    switch (ul_reason_for_call)
	{
		case DLL_PROCESS_ATTACH:
		case DLL_THREAD_ATTACH:
		case DLL_THREAD_DETACH:
		case DLL_PROCESS_DETACH:
			break;
    }
    return TRUE;
}


// Dies ist ein Beispiel für eine exportierte Variable
MSVC6LOVEPDAUDIO_API int nMsvc6lovepdaudio=0;

// Dies ist ein Beispiel für eine exportierte Funktion.
MSVC6LOVEPDAUDIO_API int fnMsvc6lovepdaudio(void)
{
	return 42;
}

// Dies ist der Konstruktor einer Klasse, die exportiert wurde.
// see msvc6lovepdaudio.h for the class definition
CMsvc6lovepdaudio::CMsvc6lovepdaudio()
{ 
	return; 
}

*/
